# Annotation Report — 2026-03-22

## Авторазметка

| Параметр | Значение |
|----------|----------|
| Модель | cross-encoder/nli-MiniLM2-L6-H768 (~120MB) |
| Задача | financial news sentiment classification |
| Порог confidence | 0.75 |
| Строк размечено | 4,119 |
| Флаги (confidence < 0.75) | 2,114 (51.3%) |

## Распределение меток (авторазметка NLI)

| Класс | Количество | Доля |
|-------|-----------|------|
| positive | 2,330 | 56.6% |
| negative | 1,706 | 41.4% |
| neutral | 83 | 2.0% |

> **Примечание:** NLI-модель предсказывает `neutral` крайне редко (~2%) —
> это известное ограничение zero-shot классификации через NLI.
> Модель хорошо различает positive/negative, но плохо выделяет нейтральный контекст
> без явного противопоставления. Для финансовых новостей рекомендуется FinBERT.

## Confidence

| Метрика | Значение |
|---------|---------|
| Mean | 0.732 |
| Std | 0.167 |
| Min | 0.347 |

## HITL — Human-in-the-Loop

| Параметр | Значение |
|----------|----------|
| Файл для правки | data/labeled/low_confidence.csv |
| Строк флаговано | 2,114 (51.3%) |
| Порог | confidence < 0.75 |
| Формат правки | редактировать колонку `label` в CSV |
| Допустимые значения | negative, neutral, positive |

После ручной правки файла пайплайн автоматически применяет изменения
через join по колонке `text` и обновляет `data/labeled/labeled.csv`.

## Выходные файлы

| Файл | Описание |
|------|---------|
| `data/labeled/labeled.csv` | Финальный размеченный датасет (4,119 строк) |
| `data/labeled/low_confidence.csv` | Строки для ручной проверки (2,114 строк) |
| `data/labeled/labelstudio_import.json` | Импорт для LabelStudio |
| `data/labeled/annotation_spec.md` | Спецификация разметки с примерами и граничными случаями |

## Cohen's Kappa

Автоматический расчёт Cohen's κ возможен после ручной разметки:
передать `--reference-col human_label` в `agents/annotation_agent.py`.
До ручной разметки метрика недоступна.

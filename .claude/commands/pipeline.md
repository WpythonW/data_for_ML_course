---
name: pipeline
description: Запустить полный ML-пайплайн (сбор → чистка → разметка → active learning) с HITL checkpoint'ами в основном чате.
---

Read the file `skills/data_pipeline.md` and follow it exactly to run the full 4-stage ML data pipeline with human-in-the-loop checkpoints. Orchestrate all stages yourself in the main chat — do NOT delegate HITL to subagents.
